# Дизайн «Офлайн-переводчика»

Источник метода: «Набор для Claude» (дизайн-система «на чёрном», грамматика
дашбордов, микровзаимодействия). Этот файл — источник правды по виду.
Если код противоречит ему — устарел код.

## Решения (с датой и причиной)

- 2026-09-22 — Android и Windows переходят на единую палитру «на чёрном» с
  янтарным акцентом #F2A03C — потому что тёплая иконка приложения уже янтарная
  (выбор пользователя), чёрный даёт контраст и бережёт OLED/батарею.
- 2026-09-22 — кнопки — таблетки (радиус 999); поля r11; карточки r14; у кнопок
  и полей нет рамок — разделение сменой поверхности.
- 2026-09-22 — главная кнопка на экране одна: «Перевести» (заливка акцентом,
  чёрный текст). «Копировать», «⇄», «Модели» — второстепенные, поверхность field.
- 2026-09-22 — статус пишется словами + цветом (зелёный «Готово ✓» — только
  вердикт); цвет без слова запрещён.
- 2026-09-22 — кнопка «Перевести» со своим ходом: «Перевод…» во время работы,
  «Готово ✓» зелёным по завершении, возврат через 1,5 с. Отказ — красным с
  причиной в статусе.

## Токены (Android, тёмная — базовая)

```
bg      #000000   фон
surface #1C1C1E   карточка
field   #2C2C2E   поле ввода, второстепенная кнопка
text    #FFFFFF
text2   #8E8E93   подпись, статус-строка
accent  #F2A03C   единственный акцент; текст на нём чёрный
accent2 #B87524   нажатый акцент (не прозрачность!)
green   #30D158   вердикт «готово»
red     #FF453A   вердикт «отказ»
```

Светлая (адаптация): bg #F2F2F7, surface #FFFFFF, field #E5E5EA, text #1C1C1E,
text2 #6E6E73, акценты те же, green #1E9E4A.

Windows-тема «Янтарь» — те же значения (bg #000000, text_bg #1C1C1E, dst #161618,
field/btn #2C2C2E, акцент #F2A03C, радиус 14).

## Типографика (Android)

- Заголовок экрана 28sp / 700 / letterSpacing -0.02em
- CAPS-надзаголовок секции 12sp / 600 / letterSpacing 0.08em / text2
- Основной текст 16sp / 400, перевод 17sp / 400
- Подпись-статус 13sp / 400 / text2 или вердиктный цвет

## Отступы

Поле экрана 20 · между секциями 16 · внутри строки 12 · внутри карточки 14.
Промежуточных значений нет.

## Радиусы

Карточка 14 · поле 11 · кнопка/чип/сегмент 999 (таблетка). Две шкалы на одном
элементе не смешивать.

## Компоненты

- Сегмент направления: переключатель-таблетки, активная залита акцентом.
- Статус-строка: слово + цвет (зелёный успех / красный отказ / text2 нейтраль).
- Карточка перевода = «лист» r14; поле ввода r11.

## Микровзаимодействия

- Анимируются только transform/opacity (не применяется пока — Android View-система,
  изменения текста и цвета мгновенные; к замене на MotionLayout).
- Отказ виден строкой в статусе, не молчанием.

## Открытые вопросы

- Светлая тема: остаётся адаптацией (набор описывает только «на чёрном»).
- Рамки: на Android не используем вовсе; на Windows остались в dark/light темах —
  допустимо (платформенная разница), «Янтарь» рамок не показывает.

<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.OfflineTranslator" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@color/on_accent</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="colorSurface">@color/card</item>
        <item name="colorOnSurface">@color/text</item>
        <item name="colorOutline">@color/border</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Таблетка для кнопок (design.md: радиус 999) -->
    <style name="ShapePill" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Поле r11 -->
    <style name="ShapeField" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">11dp</item>
    </style>

    <!-- Карточка r14 -->
    <style name="ShapeCard" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">14dp</item>
    </style>
</resources>
